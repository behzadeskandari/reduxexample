import { MODAL_OPEN,MODAL_CLOSE } from './actions';


const defaultState ={
    name : 'modalReducer',
}


export default function reducer(state =defaultState, action){
    switch (action.type) {
        case 1:
            
            break;
    
        default:
            break;
    }

    return state

}